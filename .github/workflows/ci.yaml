name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v3
 
    - name: dotnet build
      run: dotnet build TicTacToe.sln -c Release
      
    - name: dotnet test
      run: dotnet test TicTacToe.sln -c Release --no-build
    
    - name: Run automated unit and integration tests
      run: dotnet test TicTacToeAPI.UnitTests/TicTacToeAPI.UnitTests.csproj /p:CollectCoverage=true /p:CoverletOutput=TestResults/ /p:CoverletOutputFormat=lcov
    
    - name: Publish coverage report to coveralls.io
      uses: coverallsapp/github-action@master
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        path-to-lcov: ${{ github.workspace }}/